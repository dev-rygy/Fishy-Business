switch (self.swim_state) {
	case 0: // Swimming
		//self.direction = arctan(self.objective.y/self.objective.x);
		// Move towards the objective
		break;
	case 1: // Exiting
		
		// Move towards the objective
		break;
}